/**
 *0-positive_or_negative.c determines if the number stored
 *in variable n is positive, negative or zero
 *
 *
 */

int main (void)
{
int n;
if (n<0)
	printf(n, "is negative");
else if (n>0)
	printf(n, "is positive");
else 
	printf(n, "is zero");		
		
return(0);
}
